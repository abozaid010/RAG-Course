fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.4

httpx==0.27.0   # async LLM calls (replace requests)
tenacity==8.2.3 # retries

chromadb==0.4.24

python-dotenv==1.0.1  # env vars
orjson==3.10.1        # faster JSON

redis==5.0.3          # optional caching